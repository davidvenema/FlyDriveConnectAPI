version: 1.0
runtime: python3

build:
  commands:
    pre-build:
      - echo "Starting build..."
    build:
      - pip3 install --upgrade pip
      - pip3 install -r requirements.txt
    post-build:
      - echo "Build completed."

run:
  command: uvicorn main:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
  env:
    - name: DATABASE_URL
      value: "postgresql://neondb_owner:npg_xyeQnWV81Dsz@ep-floral-fire-a70lnxlg-pooler.ap-southeast-2.aws.neon.tech/neondb?sslmode=require"
    - name: APP_ENV
      value: "production"
    - name: TZ
      value: "Australia/Melbourne"
